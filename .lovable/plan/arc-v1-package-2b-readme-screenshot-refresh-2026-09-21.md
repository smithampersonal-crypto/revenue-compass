# ARC v1 — Package 2B: README Screenshot Refresh

## Scope

Replace the three existing README screenshots in place while preserving `README.md` byte-for-byte and making no application, theme, accounting, AI, document, auth, persistence, database, routing, or CI changes.

## Capture set

1. **Recruiter entry point** — current ARC Home page at a focused desktop viewport, showing the light finance-forward identity, positioning, and all three starting choices.
2. **AI interpretation and evidence** — a genuine existing synthetic AI-analyzed scenario with source-linked evidence and accountant review controls; do not use Horizon or manufacture state.
3. **Deterministic output** — Horizon Revenue Schedule with populated monthly values and sufficient surrounding ARC context.

## Image handling

- Overwrite only the three accepted PNG paths under `docs/assets/`.
- Crop each capture for README-scale legibility and remove unnecessary PNG metadata without adding dependencies.
- Add no fourth screenshot and do not rename image paths.

## Verification

- Inspect the three images side-by-side for a coherent Entry → AI control → deterministic output narrative.
- Verify README references resolve and render with GitHub-compatible Markdown.
- Scan visible text and image metadata/OCR for secrets, private information, account details, emails, internal IDs, and browser/editor chrome.
- Confirm the final diff contains only the three PNG assets and, if useful, `roadmap.md` completion tracking.
- Run applicable formatting/document checks and inspect both GitHub Actions jobs if available.
- Produce a clean repository ZIP and report final image dimensions, sizes, revision, and scope confirmations.
